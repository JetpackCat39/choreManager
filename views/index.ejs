<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <% include header.ejs %>
    <div class="taskContainer left">
        <% for(var i = 0; i < taskList.length; i++) { %>
          <h2><%= taskList[i].desc %></h2>
          <h3><%= taskList[i].reward %></h3>
        <% } %>
    </div>
    <% include footer.ejs %>
  </body>
</html>
